<!--
Compression profile — optional, agent-specific.

Any text in this file OUTSIDE HTML comments (like this block) is appended to the
built-in summarization instructions when this agent's context is compacted. The
built-in prompt already covers the general rules: which transient state to
preserve, citing the tightest message range, one fact per item, and not
restating the system prompt — so do not repeat those here.

Add ONLY guidance unique to this agent's role, for example:

  This agent manages software projects: always preserve active PR numbers,
  branch names, and worker dispatch IDs.

Leave this file comment-only (or delete it) to use the built-in behavior.
-->
